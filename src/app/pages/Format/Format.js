import React, { useEffect, useState } from 'react';
import { useFormat } from './useFormat';

const Format = ({ setLoading, sweetAlerts }) => {

  const { } = useFormat({ setLoading, sweetAlerts })

  return (
    <>
    </>
  );
};

export default Format;